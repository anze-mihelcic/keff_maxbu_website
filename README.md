# Interactive keff vs. maxbu calculations

This repository is generated by `analyze_keff_maxbu_correlation.py`. The landing page lets visitors choose a calculation; each calculation has separate data, plot scale, and axis units.
